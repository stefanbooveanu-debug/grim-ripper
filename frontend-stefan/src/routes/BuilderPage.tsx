import { PayloadBuilder } from '../components/builder/PayloadBuilder';
import '../components/shared.css';

export function BuilderPage() {
  return (
    <div className="builder-page">
      <PayloadBuilder />
    </div>
  );
}
